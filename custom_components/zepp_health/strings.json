{
  "config": {
    "step": {
      "user": {
        "title": "Zepp Health Import",
        "description": "How would you like to provide the export file?",
        "menu_options": {
          "upload": "Upload JSON file",
          "file_path": "Provide file path (advanced)"
        }
      },
      "upload": {
        "title": "Upload Zepp Health Export",
        "description": "Upload the zepp_health_export.json file generated by the extraction script.",
        "data": {
          "file": "Export JSON file"
        }
      },
      "file_path": {
        "title": "File Path",
        "description": "Provide the path to the zepp_health_export.json file on the HA server.",
        "data": {
          "file_path": "Path to zepp_health_export.json"
        }
      }
    },
    "error": {
      "file_not_found": "File not found at the specified path.",
      "invalid_json": "The file is not valid JSON.",
      "invalid_format": "The file doesn't look like a Zepp Health export (missing 'data' key).",
      "unknown": "An unexpected error occurred."
    }
  },
  "services": {
    "import_history": {
      "name": "Re-import History",
      "description": "Re-import all historical data from the previously stored export file."
    },
    "import_file": {
      "name": "Import File",
      "description": "Upload a new zepp_health_export.json and import all historical data into Home Assistant.",
      "fields": {
        "file": {
          "name": "Export File",
          "description": "The zepp_health_export.json file to import."
        }
      }
    }
  }
}
